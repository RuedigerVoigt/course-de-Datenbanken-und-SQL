# Wenn die Datenbank auf derselben Maschine läuft wie
# dieser Script, dann ist der hostname in der Regel
# localhost oder 127.0.0.1
DB_HOSTNAME = 'example.com'

# zu welcher Datenbank wollen Sie verbinden?
DB_DATABASE = 'nameOfTheDatabase'

# CREDENTIALS
# Ideal ist ein Account, der nur die nötigen Rechte
# auf der Datenbank hat.
DB_USERNAME = 'nameOfAccount'
DB_PASSPHRASE = 'yourVerySecretPassphrase'
